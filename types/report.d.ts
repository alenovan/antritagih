type GMVReport = {
  month: string;
  gmv: number;
};

type NettRevenueReport = {
  month: string;
  nett_revenue: number;
};

type ChannelCost = {
  month: string;
  channel: string;
  cost: number;
};

type ChannelEffectiveness = {
  month: string;
  channel: string;
  effectiveness_score: number;
};
